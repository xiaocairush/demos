<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.qunar.qparenting.dao.PraiseDao">

    <resultMap id="BaseResultMap" type="com.qunar.qparenting.model.bean.PraiseBean">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="article_id" property="articleId" jdbcType="INTEGER"/>
        <result column="user_name" property="userName" jdbcType="VARCHAR"/>
        <result column="praise_time" property="praiseTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, article_id, user_name, praise_time
    </sql>

    <select id="selectByArticleIdAndUserName" resultType="java.lang.Integer">
        select
        count(*)
        from q_praise
        where article_id = #{param1,jdbcType=INTEGER} and user_name = #{param2,jdbcType=VARCHAR}
    </select>

    <delete id="deleteByArticleIdAndUserName">
        delete from q_praise
        where article_id = #{param1,jdbcType=INTEGER} and user_name = #{param2,jdbcType=VARCHAR}
    </delete>

    <insert id="insert" parameterType="com.qunar.qparenting.model.bean.PraiseBean">
        insert into q_praise ( article_id, user_name)
        values ( #{articleId,jdbcType=INTEGER}, #{userName,jdbcType=VARCHAR})
    </insert>

</mapper>